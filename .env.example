# Hosted Supabase project — wishes are stored in the constellations table.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional: client-side OpenAI moderation (falls back to local checks if unset)
VITE_OPENAI_API_KEY=
